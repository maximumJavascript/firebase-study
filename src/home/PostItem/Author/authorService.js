// import React from 'react';
// import { userService } from '../../../usersService/UserService';
// import { makeAutoObservable, makeObservable, observable, runInAction, toJS } from 'mobx';
// class AuthorService {
// author = null;
// findTheAuthor = (userList, userId) => {
// return userList.find((user) => user.userUid === userId);
// };
// getAuthor = (user) => {
// user.then((user) => (this.author = user));
// user.then((user) => console.log(user));
// console.log('внутри гет афтер and author ===', this.author);
// return this.author;
// };
// }
//
// export const authorService = new AuthorService();
